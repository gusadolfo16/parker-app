import { IoClose } from 'react-icons/io5';

export default function IoCloseIcon({ size, className, title }: { size?: number, className?: string, title?: string }) {
  return <IoClose size={size} className={className} title={title} />;
}
