
import { FaCircle } from 'react-icons/fa';

export default function FaCircleIcon({ size, className, style }: { size: number, className: string, style: React.CSSProperties }) {
  return <FaCircle size={size} className={className} style={style} />;
}
