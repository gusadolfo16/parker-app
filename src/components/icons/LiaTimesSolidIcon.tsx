import { LiaTimesSolid } from 'react-icons/lia';
import Icon from '@/components/primitives/Icon';

export default function LiaTimesSolidIcon(props: {
  size?: number,
  className?: string,
}) {
  return <Icon {...props}><LiaTimesSolid /></Icon>;
}
