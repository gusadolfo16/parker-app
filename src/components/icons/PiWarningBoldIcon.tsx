import { IconBaseProps } from 'react-icons';
import { PiWarningBold } from 'react-icons/pi';

export default function PiWarningBoldIcon(props: IconBaseProps) {
  return <PiWarningBold {...props} />;
}