import { BiSun } from 'react-icons/bi';

export default function BiSunIcon({ size, className, title }: { size?: number, className?: string, title?: string }) {
  return <BiSun size={size} className={className} title={title} />;
}