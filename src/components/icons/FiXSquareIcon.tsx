import { FiXSquare } from 'react-icons/fi';

export default function FiXSquareIcon({ size, className }: { size: number, className: string }) {
  return <FiXSquare size={size} className={className} />;
}
