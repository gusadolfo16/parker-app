import { CgClose } from 'react-icons/cg';

export default function CgCloseIcon({ size, className, title }: { size?: number, className?: string, title?: string }) {
  return <CgClose size={size} className={className} title={title} />;
}
