import { HiOutlinePhotograph } from 'react-icons/hi';

export default function HiOutlinePhotographIcon({ size, className }: { size: number, className: string }) {
  return <HiOutlinePhotograph size={size} className={className} />;
}
