import { BiMoon } from 'react-icons/bi';

export default function BiMoonIcon({ size, className, title }: { size?: number, className?: string, title?: string }) {
  return <BiMoon size={size} className={className} title={title} />;
}