
import { BiErrorAlt } from 'react-icons/bi';

export default function BiErrorAltIcon({ size, className }: { size?: number, className?: string }) {
  return <BiErrorAlt size={size} className={className} />;
}
